[package]
name = "holdem-solver-abstraction"
version = "0.1.0"
edition = "2021"
license = "MIT"
rust-version = "1.75"

[dependencies]
holdem-equity = { path = "../equity" }
holdem-ranges = { path = "../ranges" }

[dev-dependencies]
holdem-cards = { path = "../cards" }