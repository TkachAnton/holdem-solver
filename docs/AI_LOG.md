# AI_LOG — память между сменами агента

## Формат записи (не менять без записи в DECISIONS)
### Сессия N — дата
Цель: <одно предложение>
Сделано: <изменения с путями>
Решения: <ссылки на D-записи>
Открытые вопросы: <что не закрыто>
Следующий шаг: <с чего начать N+1>
Замеры: <время/память/эксплуатируемость, если снимались>

---

### Сессия 1 — 2026-09-13
Цель: каркас дисциплины + подготовка первой задачи кода (ICM).
Сделано: созданы ROADMAP.md, DECISIONS.md, AI_LOG.md (фаза 0, T0.1-T0.3).
         Рабочая среда: Windows, PowerShell для скриптов агента, Rust 1.75 toolchain.
         Первая попытка скрипта упала на экранировании $ в двойных кавычках —
         исправлено переходом на одинарные here-строки (замена 2026-09-13);
         класс ошибки устранён по корню, файлы не пострадали.
         Договорённость: агент читает репозиторий сам при рабочем доступе;
         при его отсутствии пользователь вставляет файлы текстом в чат.
Решения: D-001 ... D-008 (см. DECISIONS.md).
Открытые вопросы: Q-1 (дрилл-UI), Q-2 (потолок машины), вид типа Chips (D-008).
Следующий шаг: сессия 2 — код crates/icm (T1.1): сначала чтение трёх файлов
                (корневой Cargo.toml; файл с типом Chips; crates/cards целиком),
                затем полный код крейта с тестами на эталоны и инвариант суммы $EV.
Замеры: пока нет (первый настоящий замер — T3.0).

### Сессия 2 — 2026-09-13
Цель: T1.1 — крейт crates/icm, точный ICM (Malmuth-Harville).
Сделано:
- crates/icm: icm_equity() — DP по подмножествам (O(2^n * n)), 8 тестов:
  эталоны на 2/3 игроков, инвариант суммы EV, вырожденные случаи, монотонность.
- Workspace зелёный: 103 теста (95 прежних + 8 ICM), EXIT CODE: 0.
- Пойманы и исправлены: алгоритмическая ошибка v1 (формула 2-го места неверна
  для 3-го+) — до коммита; битовый баг (rest&(rest-1) как выделение бита
  вместо сброса) — тестами, точечной заменой.
- Инфраструктурные соглашения: файлы только через [IO.File]::WriteAllText
  (UTF-8 без BOM); cargo в живой сессии PS 5.1 — только через
  "cmd /c ... > file 2>&1" (кириллический путь ломает пайплайн);
  никаких exit в inline-скриптах (убивает консоль).
Решения: подтверждён D-008 (f64 внутри ICM; Chips = i64 на входах).
Открытые вопросы: bubble_factor вынесен из T1.1 в T1.2 — нужна семантика
  переходов стека (выигрыш/проигрыш all-in, вылет vs переживание).
Следующий шаг: сессия 3 — T1.2: два варианта семантики с числами,
  HU-эталон (bf = 1.0 вне баббла), затем T1.3 CLI-команда icm.
Замеры: ICM на 8 игроков в тестах ~0.00s. Полный workspace ~2 мин,
  доминируют integration-тесты solver-core (40s + 55s) — учесть в T3.0.

### Сессия 3 — 2026-09-13
Цель: T1.2 — bubble factor (all-in и маргинальный).
Сделано:
- crates/icm: all_in_bubble_factor() — $EV текущий/выигрыш/проигрыш против
  конкретного оппонента, эффективный стек, явная семантика вылетов
  (последнее место, приз при m == n, усечённые призы остальным).
- marginal_bubble_factor() — цена следующей фишки (delta < min стеков).
- +7 тестов с ручными эталонами (BF = 1 в HU; 20/13 на баббле 3х равных;
  16/15 когда hero покрывает; 57/37 когда hero короче и вылетает с призом
  за 3-е место). Итого в крейте 15 тестов, workspace зелёный (110).
Решения: D-009 (индексы, не значения стеков; семантика вылета).
Открытые вопросы: T1.3 CLI — какие метрики показывать первыми (решение
  пользователя: all-in BF по умолчанию?).
Следующий шаг: сессия 4 — T1.3: CLI-команда icm в solver-cli с конвертом
  {"ok":true} (формат согласовать с существующими командами validate/solve),
  затем закрытие фазы 1 и переход к фазе 2 (пуш/фолд HU).
Замеры: без изменений (микросекунды на 8 игроков).

### Сессия 4 — 2026-09-13
Цель: T1.3 — CLI-команда icm.
Сделано:
- crates/solver-cli: добавлена команда icm (--stacks, --payouts, --hero,
  --villain, --delta, --json). Вывод: базовый ICM, all-in BF, маргинальный BF.
- Cargo.toml solver-cli: зависимость holdem-solver-icm.
- Workspace зелёный (110 тестов), build успешен.
Решения: формат CLI соответствует существующим командам (validate/solve).
Открытые вопросы: нет.
Следующий шаг: закрытие фазы 1 (T1.1, T1.2, T1.3 готовы) и переход к
фазе 2 (пуш/фолд HU). Обновить ROADMAP: фаза 1 -> [x].
Замеры: без изменений.

### Сессия 5 (продолжение) — 2026-09-13
Цель: T2.1 — откат на fictitious play после исчерпания двух попыток.
Сделано:
- Диагноз провала v2: в дискретной игре чистой фиксированной точки нет,
  чистые BR циклят; «финальный проход» перезаписывал только EV, не действия.
- crates/pushfold v3: FP-солвер (средние стратегии + чистый BR против них),
  рекомендация = BR против финальных средних, метрика = exploitability.
- Эквити-матрица полуквадратом с зеркалированием — вдвое быстрее,
  тяжёлый тест поднят до 200 бордов на пару без роста времени.
- Экономика: полный стек S, пуш при фолде BB +1.0, при колле S*(2eq-1),
  фолд кнопки -0.5, фолд BB -1.0; порог колла BB eq > (S-1)/2S (45% на 10bb).
- Тесты: 8 (добавлена антисимметрия матрицы); exploitability <= 0.25bb
  калиброван под шум MC при 200 бордах (записано в DoD).
Решения: D-012 дополнен — порог exploitability зависит от бордов матрицы.
Следующий шаг: T2.2 (multiway блюпринт), T2.3 (экспорт/CLI pushfold).
