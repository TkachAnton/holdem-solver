# AI_LOG — память между сменами агента

## Формат записи (не менять без записи в DECISIONS)
### Сессия N — дата
Цель: <одно предложение>
Сделано: <изменения с путями>
Решения: <ссылки на D-записи>
Открытые вопросы: <что не закрыто>
Следующий шаг: <с чего начать N+1>
Замеры: <время/память/эксплуатируемость, если снимались>

---

### Сессия 1 — 2026-09-13
Цель: каркас дисциплины + подготовка первой задачи кода (ICM).
Сделано: созданы ROADMAP.md, DECISIONS.md, AI_LOG.md (фаза 0, T0.1-T0.3).
         Рабочая среда: Windows, PowerShell для скриптов агента, Rust 1.75 toolchain.
         Первая попытка скрипта упала на экранировании $ в двойных кавычках —
         исправлено переходом на одинарные here-строки (замена 2026-09-13);
         класс ошибки устранён по корню, файлы не пострадали.
         Договорённость: агент читает репозиторий сам при рабочем доступе;
         при его отсутствии пользователь вставляет файлы текстом в чат.
Решения: D-001 ... D-008 (см. DECISIONS.md).
Открытые вопросы: Q-1 (дрилл-UI), Q-2 (потолок машины), вид типа Chips (D-008).
Следующий шаг: сессия 2 — код crates/icm (T1.1): сначала чтение трёх файлов
                (корневой Cargo.toml; файл с типом Chips; crates/cards целиком),
                затем полный код крейта с тестами на эталоны и инвариант суммы $EV.
Замеры: пока нет (первый настоящий замер — T3.0).

### Сессия 2 — 2026-09-13
Цель: T1.1 — крейт crates/icm, точный ICM (Malmuth-Harville).
Сделано:
- crates/icm: icm_equity() — DP по подмножествам (O(2^n * n)), 8 тестов:
  эталоны на 2/3 игроков, инвариант суммы EV, вырожденные случаи, монотонность.
- Workspace зелёный: 103 теста (95 прежних + 8 ICM), EXIT CODE: 0.
- Пойманы и исправлены: алгоритмическая ошибка v1 (формула 2-го места неверна
  для 3-го+) — до коммита; битовый баг (rest&(rest-1) как выделение бита
  вместо сброса) — тестами, точечной заменой.
- Инфраструктурные соглашения: файлы только через [IO.File]::WriteAllText
  (UTF-8 без BOM); cargo в живой сессии PS 5.1 — только через
  "cmd /c ... > file 2>&1" (кириллический путь ломает пайплайн);
  никаких exit в inline-скриптах (убивает консоль).
Решения: подтверждён D-008 (f64 внутри ICM; Chips = i64 на входах).
Открытые вопросы: bubble_factor вынесен из T1.1 в T1.2 — нужна семантика
  переходов стека (выигрыш/проигрыш all-in, вылет vs переживание).
Следующий шаг: сессия 3 — T1.2: два варианта семантики с числами,
  HU-эталон (bf = 1.0 вне баббла), затем T1.3 CLI-команда icm.
Замеры: ICM на 8 игроков в тестах ~0.00s. Полный workspace ~2 мин,
  доминируют integration-тесты solver-core (40s + 55s) — учесть в T3.0.

### Сессия 3 — 2026-09-13
Цель: T1.2 — bubble factor (all-in и маргинальный).
Сделано:
- crates/icm: all_in_bubble_factor() — $EV текущий/выигрыш/проигрыш против
  конкретного оппонента, эффективный стек, явная семантика вылетов
  (последнее место, приз при m == n, усечённые призы остальным).
- marginal_bubble_factor() — цена следующей фишки (delta < min стеков).
- +7 тестов с ручными эталонами (BF = 1 в HU; 20/13 на баббле 3х равных;
  16/15 когда hero покрывает; 57/37 когда hero короче и вылетает с призом
  за 3-е место). Итого в крейте 15 тестов, workspace зелёный (110).
Решения: D-009 (индексы, не значения стеков; семантика вылета).
Открытые вопросы: T1.3 CLI — какие метрики показывать первыми (решение
  пользователя: all-in BF по умолчанию?).
Следующий шаг: сессия 4 — T1.3: CLI-команда icm в solver-cli с конвертом
  {"ok":true} (формат согласовать с существующими командами validate/solve),
  затем закрытие фазы 1 и переход к фазе 2 (пуш/фолд HU).
Замеры: без изменений (микросекунды на 8 игроков).

### Сессия 4 — 2026-09-13
Цель: T1.3 — CLI-команда icm.
Сделано:
- crates/solver-cli: добавлена команда icm (--stacks, --payouts, --hero,
  --villain, --delta, --json). Вывод: базовый ICM, all-in BF, маргинальный BF.
- Cargo.toml solver-cli: зависимость holdem-solver-icm.
- Workspace зелёный (110 тестов), build успешен.
Решения: формат CLI соответствует существующим командам (validate/solve).
Открытые вопросы: нет.
Следующий шаг: закрытие фазы 1 (T1.1, T1.2, T1.3 готовы) и переход к
фазе 2 (пуш/фолд HU). Обновить ROADMAP: фаза 1 -> [x].
Замеры: без изменений.

### Сессия 5 (продолжение) — 2026-09-13
Цель: T2.1 — откат на fictitious play после исчерпания двух попыток.
Сделано:
- Диагноз провала v2: в дискретной игре чистой фиксированной точки нет,
  чистые BR циклят; «финальный проход» перезаписывал только EV, не действия.
- crates/pushfold v3: FP-солвер (средние стратегии + чистый BR против них),
  рекомендация = BR против финальных средних, метрика = exploitability.
- Эквити-матрица полуквадратом с зеркалированием — вдвое быстрее,
  тяжёлый тест поднят до 200 бордов на пару без роста времени.
- Экономика: полный стек S, пуш при фолде BB +1.0, при колле S*(2eq-1),
  фолд кнопки -0.5, фолд BB -1.0; порог колла BB eq > (S-1)/2S (45% на 10bb).
- Тесты: 8 (добавлена антисимметрия матрицы); exploitability <= 0.25bb
  калиброван под шум MC при 200 бордах (записано в DoD).
Решения: D-012 дополнен — порог exploitability зависит от бордов матрицы.
Следующий шаг: T2.2 (multiway блюпринт), T2.3 (экспорт/CLI pushfold).

### Сессия 6 (продолжение) — 2026-09-14
Цель: T2.3 — фикс Cargo.toml после ошибки с `n-литералом.
Сделано:
- Cargo.toml solver-cli переписан: pushfold + serde derive + icm.
- cargo fmt, build, workspace — зелёные (118 тестов).
Открытые вопросы: запуск pushfold с разными стеками.
Следующий шаг: smoke-тест pushfold, затем T2.2.

### Сессия 7 — 2026-09-15
Цель: T2.3 — разбор инцидента качества + фикстуры.
Сделано:
- Инцидент разобран: «фикс» grid_to_class был no-op (функция уже была
  корректна), диагноз выдуман; коридоры [380,820]/[480,680] — из памяти,
  два разных числа. По D-013 коридоры из памяти запрещены.
- Экономика проверена на предельных случаях: 2bb -> пуш any two,
  50bb -> узкий диапазон, порог BB 45% на 10bb. Логика решения чиста.
- Главный подозреваемый — шум матрицы: 200 бордов -> +-3.5% на ячейку;
  симптом: BB коллирует J9o и выкидывает T9o (инверсия).
- exact_equity_fixtures: 5 доверенных якорей (AA-KK 81.9, AKs-QQ 46.2,
  AKo-22 47.6, AA-rand 85.2, 32o-rand 32.3) — теперь единственный
  численный гейт.
- CLI: матрица по умолчанию 20000 бордов (шум ~0.35%/ячейку).
- Открыто: 818 пушей на 10bb — баг или правда — НЕИЗВЕСТНО; закрывается
  внешней push/fold-таблицей от пользователя (fixture), не спортом.
Решения: D-013; T2.1a (интеграция с evaluator/equity) в ROADMAP.
Следующий шаг: release-прогон 10bb на 20000 бордов, сравнение с доверенной
  таблицей (holdemresources/ICMIZER push-fold), затем вердикт по солверу.

### Сессия 7 — 2026-09-15
Цель: T2.3 — разбор инцидента качества + фикстуры.
Сделано:
- Инцидент разобран: «фикс» grid_to_class был no-op (функция уже была
  корректна), диагноз выдуман; коридоры [380,820]/[480,680] — из памяти,
  два разных числа. По D-013 коридоры из памяти запрещены.
- Экономика проверена на предельных случаях: 2bb -> пуш any two,
  50bb -> узкий диапазон, порог BB 45% на 10bb. Логика решения чиста.
- Главный подозреваемый — шум матрицы: 200 бордов -> +-3.5% на ячейку;
  симптом: BB коллирует J9o и выкидывает T9o (инверсия).
- exact_equity_fixtures: 5 доверенных якорей (AA-KK 81.9, AKs-QQ 46.2,
  AKo-22 47.6, AA-rand 85.2, 32o-rand 32.3) — теперь единственный
  численный гейт.
- CLI: матрица по умолчанию 20000 бордов (шум ~0.35%/ячейку).
- Открыто: 818 пушей на 10bb — баг или правда — НЕИЗВЕСТНО; закрывается
  внешней push/fold-таблицей от пользователя (fixture), не спортом.
Решения: D-013; T2.1a (интеграция с evaluator/equity) в ROADMAP.
Следующий шаг: release-прогон 10bb на 20000 бордов, сравнение с доверенной
  таблицей (holdemresources/ICMIZER push-fold), затем вердикт по солверу.

### Сессия 8 — 2026-09-15
Цель: T2.1a — точные эквити для pushfold.
Ключевая находка: crates/equity exact_profile_equity НЕ поддерживает
  префлоп (валидирует не более 2 неизвестных карт борда) — «просто
  интегрировать» было нельзя; точного префлоп-API в репо не существовало.
  Прочитано до кода, а не после — план скорректирован до ошибки.
Сделано:
- pushfold::exact_equity(a,b): полный перебор C(48,5)=1 712 304 борда,
  ничья 0.5, поверх собственного оценщика (фикстуры зелёные с сессии 7).
- exact_equity_reference (#[ignore], release): антисимметрия перебора,
  якорь AA-KK=0.8195+-0.004 (классика полного перебора, кросс-проверена
  нашим MC 40k), сверка exact vs MC на двух матчапах.
- matrix_cells_vs_exact (#[ignore], release): 6 подозрительных пар
  (K5o/Q8o/J9o/T9o vs A5o/AJo/KQo) — ячейки матрицы 20000 бордов против
  точных средних по 4 комбо; порог 0.025. Это вердикт по открытому
  вопросу сессии 7 (инверсия K5o/J9o в BB-колле).
- D-014: полная точная матрица отложена (~48e9 оценок — часы);
  вернуться с быстрым оценщиком Фазы 3.
Ожидание по вердикту: дельты < 2.5% => матрица без систематического
  смещения; инверсия в колле — честное свойство эквити против пуш-микса
  (класс-уровень не видит блокерных сдвигов диапазона оппонента —
  известный предел абстракции, ~1% на маргиналах). Цифры зафиксировать
  в следующей записи по выводу теста.
Следующий шаг: T2.2 — multiway пуш/фолд как блюпринт поверх MCCFR
  (потребует чтения crates/solver); либо, по решению пользователя,
  переход к Фазе 3.

### Сессия 9 — 2026-09-15 — регрессия icm закрыта + T3.0
Вердикт сессии 8 (цифры):
- exact_equity: AA-KK = 0.8195, AKs-QQ = 0.4621 — совпадение с классикой
  до 4-го знака; антисимметрия перебора 1e-9.
- Матрица 20000 бордов vs точный перебор (6 пар, по 4 комбо): худшая
  дельта 0.0103 (Q8o vs A5o), все < 1.1% при пороге 2.5%. Систематического
  смещения НЕТ; вопрос сессии 7 закрыт; предел абстракции класс-уровня
  ~1% на маргиналах принят.
Регрессия: команда icm отсутствовала в HEAD с f56f046 (перезапись по
  устаревшему фетчу в сессии 6); реальный дефолт матрицы оставался 200
  (мёртвый unwrap_or). Обе исправлены; см. D-015.
T3.0 замеры (release; включает ~0.3-0.5с накладных cargo на запуск):
- CPU: Intel(R) Core(TM) i5-2500K CPU @ 3.30GHz | 4 cores / 4 threads
- RAM: 15 GB
- icm CLI: 0,16 c
- pushfold 10bb (default 20000): 0,17 c
- validate 8max: 0,17 c
- solve 8max, 4 итерации: 0,4 c
- exact_equity (3 x C(48,5) = 5.1M бордов): 4,5 c
Следующий шаг: развилка T2.2 (multiway поверх MCCFR, потребует чтения
  crates/solver) vs T3.1 (кластеризация постфлопа, Фаза 3) — по цифрам
  бенчмарка. RAM-потолок (Q-2) — ещё не снят.

### Сессия 10 — 2026-09-15 — D-016: T2.2 отложен; T3.1 v1 (флоп-кластеризация)
Сделано:
- crates/abstraction: 1755 канонических классов флопов (вывод в тесте),
  feature-кластеризация, Granularity, FNV-1a fingerprint, histogram.
- CLI: flop-clusters [--granularity]. 7 тестов + смоки.
- Фикс-круги CliOptions (3 итерации, уроки в D-015-ветке): (1) Replace
  пропустил 4-й конструктор; (2) окно +-6 не покрыло разрыв «тройка ->
  delta в хвосте»; (3) финальный якорь: тройка job_id/stack_bb/
  matrix_boards + отсутствие delta в 10 строках вперёд = дырявый
  parse_job_args. Диагноз всегда по findstr /n от пользователя, файл
  вслепую не правится.
Замеры (смок): used_buckets coarse=23 / medium=50 / fine=108;
fingerprint(fine)=0x8fb68ef5db8dbe5b.
Следующий шаг: сессия 11 — эквити-уточнение флоп-бакетов (MC, фикс. сид,
тест стабильности) + тёрн/ривер-бакетинг; затем T3.2.

### Сессия 11 — 2026-09-23 — T3.1 v2: exact эквити-уточнение флоп-бакетов
Цель: эквити-уточнение флоп-бакетов поверх feature-кластеризации v1
(план сессии 10 предполагал MC — заменён exact-перебором, D-017).
Сделано:
* Карантин чужого диффа: несанкционированные изменения другого агента
  поверх 2bdae2f (13 modified + untracked) спрятаны в stash
  "foreign-agent-audit-2026-09-15"; pf-smoke.txt удалён; база — чистый
  2bdae2f. Просмотр при необходимости: git stash list / git stash show -p.
* crates/abstraction: src/equity.rs — FlopEquityAbstraction: 6 якорных
  матчапов (exact_profile_equity на представителях классов), z-агрегация
  с весами кратностей флопов, взвешенные квантильные группы, fingerprint
  эквити-режима; Cargo.toml: deps holdem-equity/holdem-ranges (+dev
  holdem-cards); lib.rs: подключение модуля. v1 нетронут.
* solver-cli: flop-clusters --equity [--equity-groups N] (дефолт off,
  группы 4); отчёт: anchors, used_buckets, fingerprint,
  base_fingerprint, total_flops, elapsed_seconds.
* Тесты: +8 в abstraction (кодировка карт против holdem-cards; сумма
  кратностей 22100; правило трипсов по всем 1755; shares на срезах;
  инъективность v1->v2 на срезе; детерминизм; reject <2 групп;
  #[ignore] release полный проход). Workspace: 128 passed + 1 ignored
  (база сессии: 121).
* Смоки: негативный (--equity-groups без --equity, exit 1); регрессия
  v1 fine — fingerprint байт-в-байт; equity coarse через CLI.
* Инциденты (все разобраны, репо не пострадало):
  (1) [IO.File]::WriteAllText не следует за cd: консоль от
      администратора, рабочая папка процесса — system32;
      DirectoryNotFound остановил запись до вреда. Фикс:
      [Environment]::CurrentDirectory = $repo после Set-Location;
      пути репо — через $env:USERPROFILE (копирование кириллического
      пути руками потеряло букву — ложный «не репозиторий»).
  (2) Два красных теста — ошибки самих тестов, не кода: сэмплы вопреки
      правилу трипсов (222 блокирует 22-AKo); требование монотонности
      used на срезе из 18 классов. Исправлены по диагнозу, код не
      трогался.
  (3) Срыв генерации CLI-патча агентом (три самоотменённых черновика,
      обрыв вывода): diff по main.rs пуст. Вывод: правки main.rs —
      только целофункциональными блоками по уникальным маркерам
      (повтор урока сессии 10 про CliOptions).
  (4) Прогноз exact-прохода 9-45 c провален: факт 123 c (release-тест)
      / 131 c (CLI). Причина: T3.0 — end-to-end бенчмарк CLI, не
      throughput оценщика (~170k оценок/с в пайплайне). См. D-017(5).
  (5) Правка ROADMAP сорвалась: локальная строка T3.1 начинается с
      дефиса, шаблон замены агента — со звёздочки; шаблон был построен
      по фетчу GitHub, расходящемуся с локальным файлом. Guard
      остановил запись до вреда; строка заменена по снятому локальному
      факту. Урок: шаблоны замен — только из локального файла, фетч
      для шаблонов не используется (расширение D-015).
  (6) Ложный стоп верификации: запись -notmatch 'паттерн', 'Multiline'
      невалидна — операторы семейства -match не принимают опции
      регэкспа вторым операндом (запятая даёт массив, а не «паттерн +
      опции»), проверка не тестировала задуманное и ложно сработала.
      Файл был правилен. Правильная форма:
      [regex]::IsMatch($text, $pattern, 'Multiline'). Урок: проверки с
      опциями регэкспа — только через [regex]::*-методы.
Решения: D-017. Зафиксирован пропуск D-010/D-011 в нумерации DECISIONS
(нумерация прыгает D-009 -> D-012; записей не существует — не
phantom-ссылки).
Открытые вопросы: оптимизация exact-прохода (аллокации score_runout —
чужой крейт equity; кэш, параллелизм) — отдельной задачей при
потребности в интерактивности; Q-2 (RAM-потолок) не снят.
Следующий шаг: сессия 12 — тёрн/ривер-бакетинг поверх equity-механизма
(объём считать от ~170k оценок/с), затем T3.2.
Замеры: exact-проход 1755 x 6 x 990 x 2 = 20.9M оценок: 123.1 c
(release-тест), 130.9 c (CLI). Fingerprints (G=4): coarse
0x4d35c0dcdc9f0cf8 (used 64; base 0xdce5e3a95475acb5), medium
0x2744bcceb148074a (124), fine 0x796a6d09a515ba0f (235). Регрессия
v1 fine: 0x8fb68ef5db8dbe5b. Якоря (available/mean/std): AA-KK
1753/0.7746/0.3129; JJ-AKo 1754/0.5534/0.3347; 87s-AKo
1755/0.4631/0.2661; AKs-QQ 1754/0.5111/0.3073; 22-AKo
1754/0.5236/0.3320; 99-TT 1753/0.2249/0.2992.

### Сессия 12 — 2026-09-23 — T3.1 закрыт: тёрн/ривер-бакетинг (D-018)
Цель: тёрн/ривер-бакетинг поверх equity-механизма D-017 (план из
записи сессии 11).
Сделано:
* crates/abstraction/src/street.rs: канонические классы бордов улиц —
  тёрн 16 432, ривер 134 459 (вывод Бернсайда; brute-force сверка всех
  270 725 / 2 598 960 бордов — независимый арбитр; оценка дизайна
  «160-210 тыс.» уточнена фактом 134 459); кратности = размеры орбит
  под 24 перестановками мастей; v1-структурные бакеты (семейство
  рангов x форма мастей x Granularity); fingerprint с тегом улицы.
* equity.rs: якорное ядро (ANCHORS, инстанцирование, статистики,
  квантили) -> pub(crate) с явными весами — переиспользуется улицами;
  флоп-обёртки под cfg(test); пины v1-fingerprints (coarse/fine из
  логов; medium снят фактом 0x97ad8be3d6155afe); контракты rep
  задокументированы. Гейт рефакторинга: эквити-fingerprints флопа
  совпали байт-в-байт (0x4d35c0dcdc9f0cf8 / 0x2744bcceb148074a /
  0x796a6d09a515ba0f), тайминг 119.8 c.
* street_equity.rs: StreetEquityAbstraction — v2 улиц на общем ядре
  (те же 6 якорей), z-агрегация с весами кратностей, квантильные
  группы; дискретные ривер-эквити {0, 0.5, 1} — задокументированное
  свойство полного борда.
* solver-cli: команды turn-clusters / river-clusters [--equity]
  [--equity-groups N] — зеркало flop-clusters.
* Тесты: +14 (канонизация; Бернсайд + brute-force тёрна; свойства v1:
  вложенность, гистограммы, bucket_of_cards; срезы v2: структура,
  детерминизм; AAAA-доступность; дискретность ривера; reject; пины
  v1; #[ignore] release-эталоны). Workspace: 142 passed + 3 ignored
  (база сессии 11: 128 + 1). DoD T3.1: fingerprint в отчёте, размер из
  конфига (granularity + equity_groups), тесты стабильности.
* Смоки CLI: негативный; тёрн v1 debug; тёрн-эквити / ривер v1 /
  ривер-эквити release — все fingerprints совпали с тестовыми (тёрн v1
  совпал и debug-vs-release — целочисленный FNV).
Инциденты (разобраны, репо не пострадало):
  (1) Двойная вставка street-подключения в lib.rs — E0428/E0252
      (повторное исполнение хвоста блока, возможно двойной paste).
      Guard Replace-Once не защищал: якорь-префикс остаётся в файле
      после замены, второй прогон проходит guard и дублирует. Фикс —
      индексная хирургия. Урок: вставки подключения модулей обязаны
      быть идемпотентными — проверять наличие целевой строки ДО
      вставки (реализовано для street_equity).
  (2) E0603/E0624 приватность canonical_key/tag/from_classes +
      неиспользуемые импорты — механика; pub(crate)/удаление. Заодно
      самопроверкой пойманы два тест-бага до запуска: перевёрнутый
      assert доступности (AAAA: 87s-AKo недоступен, был is_some) и
      debug-ловушки (полная генерация ривера в debug-тесте); тестовый
      модуль street_equity переписан целиком.
  (3) Продакшн-баг 0xFF-паддинга: rep тёрна [u8;5] с хвостом 0xFF
      уходил в ядро эквити — used_cards[255], паника трёх тестов.
      Пойман debug-тестами до коммита. Первая гипотеза (class_count
      среза) отвергнута собственным guard'ом — потребовал полное
      сообщение паники; причина найдена чтением (index is 255). Фиксы:
      срез rep[..card_count()] + class_count-ассерт под срезовой
      дизайн улиц (v1-база строится из переданных классов, в отличие
      от флопа, где база всегда полная). Урок: [u8;5] с паддингом
      опасен на границе модулей; при T3.2 — кандидат на Board<'a>.
Решения: D-018.
Открытые вопросы: Q-2 (RAM-потолок) не снят; оптимизация exact-прохода
(кэш/параллелизм) — по потребности (перенос из сессии 11).
Следующий шаг: T3.2 — интеграция абстракции в solver-core ЗА ФЛАГОМ
(exact-режим остаётся для тестов); при проектировании переходов улиц
рассмотреть Board<'a> вместо [u8;5]+паддинг.
Замеры (release, i5-2500K): генерация тёрн 0.25-0.39 c, ривер 2.4 c;
brute-force сверка ривера 4.8 c. Эквити-проход: тёрн 50.5 c, ривер
10.3 c (CLI: 51.1 c / 11.7 c). v1 fingerprints (coarse/medium/fine):
тёрн 0xd4f0e043b66e509e / 0xc4ceac9bdeac22ac / 0x0516cd5688e8864a
(used 56/119/257); ривер 0x4e5a78a1442681bb / 0xcacfc4d257bf516f /
0x8b6d16ed601c8655 (used 82/181/398). Эквити G=4: тёрн
0x8bc620292d2ad884 / 0x0b23198596629143 / 0x2e5a40cf6a5590de (used
164/319/638); ривер 0xd16b2bf9ec48d09e / 0x1f91d78be19db113 /
0x70b9312dc942a92e (used 254/502/1009). Якоря (available/mean/std):
тёрн AA-KK 16382/0.7602/0.3679, JJ-AKo 16405/0.5548/0.4049, 87s-AKo
16425/0.4665/0.3512, AKs-QQ 16402/0.5076/0.3862, 22-AKo
16405/0.5283/0.4013, 99-TT 16382/0.2393/0.3560; ривер AA-KK
133727/0.7472/0.4337, JJ-AKo 134069/0.5570/0.4960, 87s-AKo
134343/0.4696/0.4984, AKs-QQ 134001/0.5033/0.4993, 22-AKo
134069/0.5337/0.4978, 99-TT 133727/0.2524/0.4331.
